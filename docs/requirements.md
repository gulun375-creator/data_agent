# Data Agent - 数据分析 AI Agent 需求文档

> **项目名称**：Data Agent  
> **文档版本**：v0.4（草案）  
> **创建日期**：2026-04-02  
> **最后更新**：2026-04-02  
> **状态**：需求收集中

---

## 1. 项目概述

### 1.1 项目目标

构建一个基于大语言模型（LLM）的数据分析 AI Agent，支持用户通过自然语言问答的方式完成数据分析任务。降低数据分析门槛，让非技术用户也能通过对话获取数据洞察。

### 1.2 目标用户

| 用户角色 | 描述 | 核心诉求 |
|---------|------|---------|
| 业务分析师 | 需要频繁查看数据指标，但 SQL/Python 能力有限 | 用自然语言快速获取数据洞察 |
| 产品经理 | 需要数据支撑决策，不愿等待数据团队排期 | 自助式数据查询和可视化 |
| 运营人员 | 需要日常监控关键指标、生成报表 | 自动化报表和异常检测 |
| 数据工程师 | 需要快速探索数据、验证假设 | 加速 EDA（探索性数据分析）流程 |

### 1.3 核心价值

- 自然语言交互，零代码门槛
- 自动生成并执行分析代码（SQL / Python）
- 结果可视化呈现（图表 + 文字解读）
- 多轮对话支持，逐步深入分析

---

## 2. 功能需求

### 2.1 数据接入

| 功能项 | 优先级 | 描述 | 状态 |
|-------|--------|------|------|
| **数据表 Schema 注册** | **P0** | **用户配置数据表结构（Schema），Agent 基于 Schema 理解数据并生成 SQL** | **已确认** |
| **SQL 生成与执行** | **P0** | **Agent 根据 Schema 上下文生成 SQL，通过数据库查询工具执行并返回结果** | **已确认** |
| CSV/Excel 文件上传 | P1 | 用户上传本地文件，Agent 自动解析 | 待确认 |

#### 2.1.1 Schema 驱动的 SQL 分析（核心流程）

这是 Agent 数据分析的核心路径：**用户提供数据表 Schema → Agent 理解表结构 → 生成 SQL → 调用数据库查询工具执行 → 返回分析结果。**

**Schema 管理**

| 功能项 | 描述 | 状态 |
|-------|------|------|
| Schema 静态配置 | 用户通过 JSON/YAML 配置文件定义表结构（表名、字段名、字段类型、字段业务含义、枚举映射、表关系） | 已确认 |
| Schema 自动发现 | 连接数据库后自动读取表结构（INFORMATION_SCHEMA / SHOW TABLES 等） | P2，后续扩展 |
| Schema UI 管理 | 通过 Web UI 动态编辑 Schema | P2，后续扩展 |

**Schema 数据结构示例**

```json
{
  "data_source": {
    "name": "电商业务库",
    "type": "mysql",
    "connection": "mysql://user:pass@host:3306/ecommerce"
  },
  "tables": [
    {
      "table_name": "orders",
      "description": "订单主表，记录所有用户订单信息",
      "columns": [
        {
          "name": "id",
          "type": "BIGINT",
          "description": "订单ID，主键",
          "is_primary_key": true
        },
        {
          "name": "user_id",
          "type": "BIGINT",
          "description": "用户ID，关联 users 表",
          "foreign_key": "users.id"
        },
        {
          "name": "total_amount",
          "type": "DECIMAL(10,2)",
          "description": "订单总金额（单位：元）"
        },
        {
          "name": "status",
          "type": "TINYINT",
          "description": "订单状态",
          "enum_values": {
            "0": "待支付",
            "1": "已支付",
            "2": "已发货",
            "3": "已完成",
            "4": "已取消"
          }
        },
        {
          "name": "created_at",
          "type": "DATETIME",
          "description": "订单创建时间"
        }
      ]
    },
    {
      "table_name": "users",
      "description": "用户表",
      "columns": [
        {
          "name": "id",
          "type": "BIGINT",
          "description": "用户ID，主键",
          "is_primary_key": true
        },
        {
          "name": "name",
          "type": "VARCHAR(100)",
          "description": "用户姓名"
        },
        {
          "name": "region",
          "type": "VARCHAR(50)",
          "description": "所在地区"
        },
        {
          "name": "created_at",
          "type": "DATETIME",
          "description": "注册时间"
        }
      ]
    }
  ],
  "relationships": [
    {
      "from": "orders.user_id",
      "to": "users.id",
      "type": "many_to_one",
      "description": "每个订单属于一个用户"
    }
  ]
}
```

**SQL 生成与执行流程**

```
用户提问
  │
  ▼
┌─────────────────────────────┐
│ 1. 意图理解                  │  Agent 解析用户自然语言问题
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 2. Schema 上下文注入         │  将相关表的 Schema 信息注入 LLM 上下文
│    - 匹配相关表              │  （根据问题智能选择涉及的表，
│    - 注入字段定义和业务含义    │   避免一次性注入全部 Schema）
│    - 注入表关系              │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 3. SQL 生成                  │  LLM 基于 Schema 上下文生成 SQL
│    - 生成标准 SQL 查询        │
│    - 自动处理 JOIN / 聚合     │
│    - 适配目标数据库方言       │
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 4. SQL 安全校验（可选）       │  防止危险操作（DROP/DELETE/UPDATE 等）
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 5. 调用数据库查询工具执行      │  通过统一的 DB Query Tool 执行 SQL
│    Tool: execute_sql()       │  支持不同数据库后端
└──────────────┬──────────────┘
               │
               ▼
┌─────────────────────────────┐
│ 6. 结果处理与呈现             │  Agent 解读查询结果
│    - 格式化为表格/图表        │  生成自然语言总结
│    - 自然语言解读             │
└─────────────────────────────┘
```

**Agent Tool 定义（草案）**

Agent 需要以下工具来完成 Schema 驱动的 SQL 分析：

| Tool 名称 | 描述 | 输入 | 输出 |
|-----------|------|------|------|
| `get_schema` | 获取指定表或全部表的 Schema 信息 | 表名（可选） | Schema JSON |
| `execute_sql` | 执行 SQL 查询并返回结果 | SQL 语句 | 查询结果（行列数据） |
| `list_tables` | 列出所有可用的数据表 | 无 | 表名列表 + 简要描述 |
| `get_sample_data` | 获取指定表的示例数据 | 表名, 行数 | 示例数据行 |

> **已确认**：Schema 通过静态配置文件（JSON/YAML）定义，MVP 阶段不做 UI 管理和自动发现。  
> **待讨论**：SQL 执行前是否强制需要用户确认？还是仅对写操作拦截？

### 2.2 问答与分析

| 功能项 | 优先级 | 描述 | 状态 |
|-------|--------|------|------|
| 自然语言转 SQL | P0 | 根据用户问题自动生成 SQL 查询 | 待确认 |
| 自然语言转 Python | P0 | 生成 Pandas 等数据分析代码并执行 | 待确认 |
| 基础统计分析 | P0 | 均值、中位数、分布、占比等 | 待确认 |
| 数据概览 | P0 | 自动识别数据结构、字段类型、数据质量 | 待确认 |
| 高级分析 | P1 | 趋势分析、相关性分析、异常检测 | 待确认 |
| 预测分析 | P2 | 基于历史数据的简单预测（时间序列等） | 待确认 |
| 多表关联分析 | P1 | 跨表 JOIN 查询和分析 | 待确认 |

> **待讨论**：Agent 是否需要在执行前向用户确认生成的 SQL/代码？

### 2.3 可视化与输出

| 功能项 | 优先级 | 描述 | 状态 |
|-------|--------|------|------|
| 文字回答 | P0 | 用自然语言总结分析结果 | 待确认 |
| 数据表格 | P0 | 以表格形式展示查询结果 | 待确认 |
| 基础图表 | P0 | 折线图、柱状图、饼图等 | 待确认 |
| 交互式图表 | P1 | 支持缩放、筛选、悬浮提示等交互 | 待确认 |
| 报告导出 | P2 | 导出为 PDF/Excel/Markdown 报告 | 待确认 |
| 仪表盘 | P3 | 自定义数据看板 | 待确认 |

> **待讨论**：图表库选型？Matplotlib（静态）vs Plotly/ECharts（交互式）？

### 2.4 对话与上下文

| 功能项 | 优先级 | 描述 | 状态 |
|-------|--------|------|------|
| 多轮对话 | P0 | 支持上下文关联的连续提问 | 待确认 |
| 对话历史 | P1 | 保存和回顾历史对话记录 | 待确认 |
| 分析记忆 | P1 | 记住之前分析的结论，后续分析可引用 | 待确认 |
| 对话分支 | P2 | 从某个节点开始新的分析方向 | 待确认 |
| 分析模板 | P2 | 常用分析场景的快捷入口 | 待确认 |

> **待讨论**：上下文窗口大小限制？是否需要长期记忆？

---

## 3. 非功能需求

### 3.1 性能

| 指标 | 目标值 | 状态 |
|------|--------|------|
| 简单查询响应时间 | < 5 秒 | 待确认 |
| 复杂分析响应时间 | < 30 秒 | 待确认 |
| 支持的最大数据量 | 待定 | 待确认 |
| 并发用户数 | 待定 | 待确认 |

### 3.2 安全

| 需求项 | 描述 | 状态 |
|-------|------|------|
| 数据库只读访问 | Agent 生成的 SQL 仅允许 SELECT 操作 | 待确认 |
| 代码沙箱执行 | 生成的 Python 代码在隔离环境中运行 | 待确认 |
| 数据脱敏 | 敏感字段自动脱敏处理 | 待确认 |
| 用户鉴权 | 登录认证和权限控制 | 待确认 |
| API Key 管理 | LLM API Key 的安全存储 | 待确认 |

> **待讨论**：安全等级要求？是否面向企业内部使用？

### 3.3 可扩展性

| 需求项 | 描述 | 状态 |
|-------|------|------|
| 多 LLM 支持 | 可切换不同的大模型后端 | 待确认 |
| 插件机制 | 支持自定义分析工具/数据源插件 | 待确认 |
| 自定义 Prompt | 允许用户自定义系统提示词 | 待确认 |

---

## 4. 技术选型（待确认）

### 4.1 技术栈候选

| 组件 | 候选方案 | 推荐 | 状态 |
|------|---------|------|------|
| **开发语言** | Python | Python | 待确认 |
| **LLM 框架** | LangChain / LlamaIndex / 原生 SDK | 待定 | 待确认 |
| **LLM 模型** | OpenAI GPT / 通义千问 / GLM / 本地模型 | 待定 | 待确认 |
| **数据处理** | Pandas / Polars | 待定 | 待确认 |
| **可视化** | Matplotlib / Plotly / ECharts | 待定 | 待确认 |
| **Web 框架** | Streamlit / Gradio / FastAPI + React | 待定 | 待确认 |
| **数据库 ORM** | SQLAlchemy | SQLAlchemy | 待确认 |
| **向量数据库** | FAISS / Chroma（用于语义搜索场景） | 待定 | 待确认 |

> **待讨论**：  
> - LLM 选哪个？是否需要支持国内模型？  
> - 前端用快速原型方案（Streamlit）还是生产级方案（React）？  
> - 是否需要向量数据库做语义搜索？

### 4.2 系统架构（草案）

```
┌──────────────────────────────────────────────────┐
│                   前端展示层                       │
│            Web UI (Streamlit / React)              │
│         对话界面 / 图表展示 / 文件上传              │
└────────────────────┬─────────────────────────────┘
                     │  HTTP / WebSocket
┌────────────────────▼─────────────────────────────┐
│                   API 服务层                       │
│              FastAPI / Flask                       │
│         路由 / 鉴权 / 会话管理 / 限流              │
└────────────────────┬─────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────┐
│                Agent 核心引擎                      │
│  ┌──────────────────────────────────────────┐     │
│  │         对话管理 & 上下文维护               │     │
│  ├──────────────────────────────────────────┤     │
│  │         意图识别 & 任务规划                 │     │
│  ├──────────────────────────────────────────┤     │
│  │         工具调度 (Tool Calling)            │     │
│  └──────────────────────────────────────────┘     │
│                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐      │
│  │ SQL 工具  │ │Python 工具│ │ 可视化工具    │      │
│  │ 生成&执行 │ │ 代码执行  │ │ 图表生成     │      │
│  └──────────┘ └──────────┘ └──────────────┘      │
└────────────────────┬─────────────────────────────┘
                     │
┌────────────────────▼─────────────────────────────┐
│                   数据层                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐      │
│  │ 文件存储  │ │ 关系数据库 │ │ 外部 API     │      │
│  │CSV/Excel │ │SQL DB    │ │ REST/GraphQL │      │
│  └──────────┘ └──────────┘ └──────────────┘      │
└──────────────────────────────────────────────────┘
```

---

## 5. 用户场景（User Stories）

### 场景 1：上传 CSV 进行快速分析

> **作为** 业务分析师  
> **我想要** 上传一份销售数据 CSV，然后用自然语言提问  
> **以便于** 快速获取数据洞察，而不需要写代码

**示例对话**：
```
用户：[上传 sales_2025.csv]
Agent：已加载数据文件，共 15,234 行 x 12 列。字段包括：日期、产品名称、销售额、地区...
      数据时间范围：2025-01 至 2025-12。是否需要我先做一个整体概览分析？

用户：上个季度销售额最高的产品是什么？
Agent：上个季度（2025 Q4）销售额 Top 5 产品如下：
      [柱状图]
      1. 产品A - ¥2,340,000
      2. 产品B - ¥1,890,000
      ...
      产品A的销售额领先第二名约23.8%，主要集中在华东地区。

用户：按月拆分看一下产品A的趋势
Agent：[折线图：产品A月度销售趋势]
      产品A在10月达到峰值（¥920,000），11月略有回落，12月恢复增长...
```

### 场景 2：基于 Schema 的数据库问答（核心场景）

> **作为** 产品经理  
> **我想要** 配置好数据库 Schema 后，用自然语言查询业务数据  
> **以便于** 自助获取产品指标，不依赖数据团队

**前置步骤**：管理员已配置好数据库连接和表 Schema（包含 `users`、`orders`、`products` 等表的字段定义和业务含义）。

**示例对话**：
```
用户：我们有哪些数据表可以查询？
Agent：当前已配置以下数据表：
      [调用 list_tables 工具]
      1. users - 用户表（字段：id, name, region, created_at）
      2. orders - 订单表（字段：id, user_id, total_amount, status, created_at）
      3. products - 商品表（字段：id, name, category, price）
      共 3 张表，是否需要查看某张表的详细结构？

用户：过去30天的日活跃用户趋势怎么样？
Agent：让我查询一下。
      [根据 Schema 生成 SQL]
      SELECT DATE(created_at) AS date, COUNT(DISTINCT user_id) AS dau
      FROM orders
      WHERE created_at >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
      GROUP BY DATE(created_at)
      ORDER BY date;
      [调用 execute_sql 工具执行]
      
      [折线图：DAU 趋势]
      过去30天平均 DAU 为 12,450，整体呈上升趋势（+8.2%）。
      3月15日出现峰值（18,200），可能与营销活动相关。

用户：这些活跃用户的地区分布呢？
Agent：[根据 Schema 识别到需要 JOIN users 表获取 region 字段]
      SELECT u.region, COUNT(DISTINCT o.user_id) AS active_users
      FROM orders o JOIN users u ON o.user_id = u.id
      WHERE o.created_at >= DATE_SUB(CURDATE(), INTERVAL 30 DAY)
      GROUP BY u.region
      ORDER BY active_users DESC;
      [调用 execute_sql 工具执行]
      
      [饼图：用户地区分布]
      华东地区占比最高（35.2%），其次是华南（22.1%）...

用户：状态为"已取消"的订单占比是多少？
Agent：[根据 Schema 中 status 字段的 enum_values 映射，知道 status=4 表示"已取消"]
      SELECT 
        COUNT(CASE WHEN status = 4 THEN 1 END) AS cancelled,
        COUNT(*) AS total,
        ROUND(COUNT(CASE WHEN status = 4 THEN 1 END) / COUNT(*) * 100, 2) AS cancel_rate
      FROM orders
      WHERE created_at >= DATE_SUB(CURDATE(), INTERVAL 30 DAY);
      [调用 execute_sql 工具执行]
      
      过去30天订单取消率为 5.8%（共 490 笔取消订单 / 8,450 笔总订单）。
      相比上月（4.2%）有所上升，建议关注取消原因。
```

> **关键点**：Agent 能够利用 Schema 中的字段含义（如 `status` 的枚举值映射）准确理解用户的自然语言描述，并生成正确的 SQL。

### 场景 3：自动生成数据报告

> **作为** 运营人员  
> **我想要** Agent 自动生成周度运营报告  
> **以便于** 减少手动整理数据的时间

**示例对话**：
```
用户：帮我生成本周的运营周报，关注 GMV、订单量、用户增长
Agent：正在分析本周数据... 已生成运营周报：
      
      📊 本周运营周报（2026-03-23 ~ 2026-03-29）
      
      一、核心指标概览
      - GMV: ¥5,230,000（环比 +12.3%）
      - 订单量: 8,450 单（环比 +8.7%）
      - 新增用户: 2,100（环比 -3.2%）
      
      二、关键发现
      ...
      
      [导出为 PDF]
```

---

## 6. MVP 范围定义（待确认）

建议 MVP（最小可行产品）包含以下功能：

- [ ] **数据表 Schema 配置与管理**（配置文件方式）
- [ ] **自然语言转 SQL 生成**（基于 Schema 上下文）
- [ ] **SQL 执行工具**（对接数据库执行查询，返回结果）
- [ ] 自然语言问答（文字回答 + 基础图表）
- [ ] 多轮对话上下文支持
- [ ] 基础 Web UI（对话界面 + 结果展示）
- [ ] 支持至少一个 LLM 后端

> **待讨论**：以上 MVP 范围是否合理？需要增减什么？

---

## 7. 里程碑规划（草案）

| 阶段 | 内容 | 状态 |
|------|------|------|
| **M0 - 需求确认** | 完成需求文档、确认技术选型 | 进行中 |
| **M1 - 核心引擎** | Agent 核心逻辑、LLM 接入、基础工具链 | 未开始 |
| **M2 - 数据接入** | CSV/Excel 解析、数据库连接 | 未开始 |
| **M3 - 可视化** | 图表生成、结果格式化 | 未开始 |
| **M4 - Web UI** | 对话界面、文件上传、图表展示 | 未开始 |
| **M5 - 优化迭代** | 性能优化、安全加固、更多数据源 | 未开始 |

---

## 8. 开放问题清单

以下问题需要在后续讨论中逐一确认：

- [x] **Q1**：MVP 阶段优先支持哪些数据源？ → **已确认：Schema 注册 + SQL 生成执行为核心，CSV/Excel 作为后续扩展**
- [ ] **Q2**：LLM 选型？OpenAI / 通义千问 / 其他？是否需要多模型支持？
- [ ] **Q3**：前端方案？Streamlit（快速）vs React（生产级）？
- [ ] **Q4**：部署形态？本地工具 vs 在线服务？
- [ ] **Q5**：是否需要用户登录和权限管理？
- [ ] **Q6**：Agent 执行代码前是否需要用户确认？
- [ ] **Q7**：数据隐私和安全等级要求？
- [ ] **Q8**：是否需要支持中文和英文双语？
- [ ] **Q9**：可视化图表的交互性要求？
- [ ] **Q10**：是否需要对接企业内部系统（如飞书、钉钉）？

---

## 变更记录

| 日期 | 版本 | 变更内容 | 作者 |
|------|------|---------|------|
| 2026-04-02 | v0.1 | 初始草案，建立文档框架 | - |
| 2026-04-02 | v0.2 | 新增 Schema 驱动的 SQL 分析核心流程、Tool 定义、场景细化 | - |
| 2026-04-02 | v0.3 | 精简数据源：聚焦 Schema+SQL 核心路径，CSV/Excel 降为 P1 后续扩展 | - |
| 2026-04-02 | v0.4 | 确认 Schema 采用静态配置文件方式，UI 管理和自动发现降为 P2 | - |
